$('#future_balance').html("<%= j render('future_reports/future_balance') %>")
$('#end_date').datepicker({
                      weekStart: 1,
                      autoclose: true,
                      format:'yyyy-mm-dd' ,
                      todayHighlight: true
                    });
